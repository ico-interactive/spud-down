name: spud-down

services:
  bot:
    environment:
      DISCORD_TOKEN:
    build:
      context: ./bot
      dockerfile: ../Dockerfile
  server:
    environment:
      SL_API_KEY:
      SL_API_URL:
      SL_MOCK_API_URL:
      DL_API_URL:
    build:
      context: ./bot
      dockerfile: ../Dockerfile
